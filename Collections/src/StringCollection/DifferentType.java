package StringCollection;

import java.util.*;

public class DifferentType {
    public static void main(String[] args) {
        //this will store strings in array
        List<String> ls=new ArrayList<>();
        //dublicated not allowed and preserve insertion order doubly linked list is maintained order
        LinkedHashSet<String> st=new LinkedHashSet<>();

        //dublicated not allowed and store values in key value pair
        Set<String> st2=new HashSet<>();
        //preserve natural order
        Set<String> st3=new TreeSet<>();

    }
}
